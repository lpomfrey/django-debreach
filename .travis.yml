language: python
python:
    - "2.7"
install:
    - "pip install -r requirements.txt --use-mirrors"
before_script:
    - "pip install python-coveralls"
script:
    - "./run_tests.sh"
after_success:
    - "coveralls"
notifications:
    email: false
